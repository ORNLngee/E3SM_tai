set(CXX_LINKER "CXX")
set(NETCDF_PATH "$ENV{NETCDF_PATH}")
set(NETCDF_C_PATH "$ENV{NETCDF_C_PATH}")
set(NETCDF_FORTRAN_PATH "$ENV{NETCDF_FORTRAN_PATH}")
set(HDF5_PATH "$ENV{HDF5_PATH}")
set(BLASLAPACK_LIBDIR "$ENV{BLASLAPACK_LIBDIR}")
string(APPEND FFLAGS " -fallow-argument-mismatch")
string(APPEND CFLAGS " -I${NETCDF_C_PATH}/include -I${HDF5_PATH}/include")
string(APPEND FFLAGS " -I${NETCDF_FORTRAN_PATH}/include -I${NETCDF_C_PATH}/include -I${HDF5_PATH}/include")
string(APPEND LDFLAGS " -framework Accelerate")
string(APPEND LDFLAGS " -Wl,-rpath,$ENV{FC_ROOT}/lib")
if (COMP_NAME STREQUAL cpl)
  string(APPEND LDFLAGS " -L${BLASLAPACK_LIBDIR} -llapack -lblas")
endif()
execute_process(COMMAND ${NETCDF_C_PATH}/bin/nc-config --libs OUTPUT_VARIABLE SHELL_CMD_OUTPUT_BUILD_INTERNAL_IGNORE0 OUTPUT_STRIP_TRAILING_WHITESPACE)
string(APPEND SLIBS " ${SHELL_CMD_OUTPUT_BUILD_INTERNAL_IGNORE0}")
execute_process(COMMAND ${NETCDF_FORTRAN_PATH}/bin/nf-config --flibs OUTPUT_VARIABLE SHELL_CMD_OUTPUT_BUILD_INTERNAL_IGNORE0 OUTPUT_STRIP_TRAILING_WHITESPACE)
string(APPEND SLIBS " ${SHELL_CMD_OUTPUT_BUILD_INTERNAL_IGNORE0}")
string(APPEND SLIBS " ${HDF5_PATH}/lib")

# make sure both ALQUIMIA_PATH and PFLOTRAN_PATH are in user's ENVs
# such as:
# export ALQUIMIA_PATH=/Users/f9y/ELM-PFLOTRAN_ROOT/install/v2021_mpich/alquimia
# export PFLOTRAN_PATH=/Users/f9y/ELM-PFLOTRAN_ROOT/install/v2021_mpich/pflotran
set(ALQUIMIA_PATH $ENV{ALQUIMIA_PATH})
set(PFLOTRAN_PATH $ENV{PFLOTRAN_PATH})
set(PETSC_DIR $ENV{PETSC_DIR})
if (ALQUIMIA_PATH AND PFLOTRAN_PATH AND PETSC_DIR)
  if (COMP_NAME STREQUAL "elm")
    string(APPEND CPPDEFS " -DUSE_ALQUIMIA_LIB")
    string(APPEND CFLAGS " -I${ALQUIMIA_PATH}/include")
    string(APPEND CXXFLAGS " -I${ALQUIMIA_PATH}/include")
    string(APPEND FFLAGS " -I${ALQUIMIA_PATH}/include -I${PFLOTRAN_PATH}")
  endif()
  if (COMP_NAME STREQUAL "cpl")
    string(APPEND SLIBS " -Wl,-rpath,${ALQUIMIA_PATH}/lib -L${ALQUIMIA_PATH}/lib -lalquimia")
    string(APPEND SLIBS " -Wl,-rpath,${PFLOTRAN_PATH} -L${PFLOTRAN_PATH} -lpflotranchem")
    string(APPEND SLIBS " -Wl,-rpath,${PETSC_DIR}/lib -L${PETSC_DIR}/lib -lpetsc")
  endif()
endif()
